Setting up flask
===
    mkdir data_crosstrainer
    cd data_crosstrainer
    git init
    echo venv >> .gitignore
    virtualenv venv
    source venv/bin/activate
    pip install Flask
    pip install requests
    
